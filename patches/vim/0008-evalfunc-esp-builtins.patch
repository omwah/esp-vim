--- a/src/evalfunc.c	2026-09-21 13:40:41.000000000 -0700
+++ b/src/evalfunc.c	2026-09-24 23:08:59.526322859 -0700
@@ -14,6 +14,11 @@
 
 #include "vim.h"
 
+#ifdef ESP_PLATFORM
+// The esp_*() builtins, implemented by the ESP32 port (esp-vim/components/vim).
+# include "esp_vim_api.h"
+#endif
+
 #if defined(FEAT_EVAL)
 
 #ifdef VMS
@@ -2279,6 +2284,123 @@
 			ret_void,	    f_err_teapot},
     {"escape",		2, 2, FEARG_1,	    arg2_string,
 			ret_string,	    f_escape},
+#ifdef ESP_PLATFORM
+    // Keep these sorted: lookup is a binary search over this table, and an
+    // out-of-order row breaks unrelated builtins.  All "esp_" names sort
+    // between "escape" and "eval".
+    {"esp_adc_read",	1, 1, FEARG_1,	    arg1_number,
+			ret_dict_any,	    f_esp_adc_read},
+    {"esp_ble_scan",	0, 1, FEARG_1,	    arg1_number,
+			ret_list_dict_any,  f_esp_ble_scan},
+    {"esp_bt_keyboard",	0, 0, 0,	    NULL,
+			ret_dict_any,	    f_esp_bt_keyboard},
+    {"esp_bt_keyboard_forget", 0, 0, 0,	    NULL,
+			ret_bool,	    f_esp_bt_keyboard_forget},
+    {"esp_bt_keyboard_pair", 1, 2, FEARG_1,  arg2_string,
+			ret_bool,	    f_esp_bt_keyboard_pair},
+    {"esp_console_output", 0, 1, FEARG_1,  arg1_bool,
+			ret_bool,	    f_esp_console_output},
+    {"esp_display",	0, 0, 0,	    NULL,
+			ret_dict_any,	    f_esp_display},
+    {"esp_display_font", 0, 1, FEARG_1,  arg1_string,
+			ret_string,	    f_esp_display_font},
+    {"esp_fs_copy",	2, 3, FEARG_1,	    arg3_string_string_bool,
+			ret_bool,	    f_esp_fs_copy},
+    {"esp_fs_delete",	1, 1, FEARG_1,	    arg1_string,
+			ret_bool,	    f_esp_fs_delete},
+    {"esp_fs_info",	1, 1, FEARG_1,	    arg1_string,
+			ret_dict_any,	    f_esp_fs_info},
+    {"esp_fs_list",	1, 1, FEARG_1,	    arg1_string,
+			ret_list_dict_any,  f_esp_fs_list},
+    {"esp_fs_mkdir",	1, 1, FEARG_1,	    arg1_string,
+			ret_bool,	    f_esp_fs_mkdir},
+    {"esp_fs_move",	2, 3, FEARG_1,	    arg3_string_string_bool,
+			ret_bool,	    f_esp_fs_move},
+    {"esp_fs_roots",	0, 0, 0,	    NULL,
+			ret_list_dict_any,  f_esp_fs_roots},
+    {"esp_gpio_mode",	2, 2, FEARG_1,	    arg2_number_string,
+			ret_bool,	    f_esp_gpio_mode},
+    {"esp_gpio_pins",	0, 0, 0,	    NULL,
+			ret_list_number,    f_esp_gpio_pins},
+    {"esp_gpio_read",	1, 1, FEARG_1,	    arg1_number,
+			ret_number,	    f_esp_gpio_read},
+    {"esp_gpio_write",	2, 2, FEARG_1,	    arg2_number,
+			ret_bool,	    f_esp_gpio_write},
+    {"esp_heap",	0, 0, 0,	    NULL,
+			ret_dict_any,	    f_esp_heap},
+    {"esp_http_get",	1, 2, FEARG_1,	    arg2_string,
+			ret_dict_any,	    f_esp_http_get},
+    {"esp_i2c_scan",	0, 2, FEARG_1,	    arg2_number,
+			ret_list_number,    f_esp_i2c_scan},
+    {"esp_info",	0, 0, 0,	    NULL,
+			ret_dict_any,	    f_esp_info},
+    {"esp_net_status",	0, 0, 0,	    NULL,
+			ret_dict_any,	    f_esp_net_status},
+    {"esp_nvs_erase",	2, 2, FEARG_1,	    arg2_string,
+			ret_bool,	    f_esp_nvs_erase},
+    {"esp_nvs_get",	2, 3, FEARG_1,	    NULL,
+			ret_any,	    f_esp_nvs_get},
+    {"esp_nvs_list",	0, 1, FEARG_1,	    arg1_string,
+			ret_list_dict_any,  f_esp_nvs_list},
+    {"esp_nvs_set",	3, 3, FEARG_1,	    NULL,
+			ret_bool,	    f_esp_nvs_set},
+    {"esp_reboot",	0, 0, 0,	    NULL,
+			ret_void,	    f_esp_reboot},
+    {"esp_sensors",	0, 2, FEARG_1,	    arg2_number,
+			ret_list_dict_any,  f_esp_sensors},
+    {"esp_serial_close", 1, 1, FEARG_1,	    arg1_number,
+			ret_void,	    f_esp_serial_close},
+    {"esp_serial_open",	2, 4, FEARG_1,	    NULL,
+			ret_bool,	    f_esp_serial_open},
+    {"esp_serial_read",	1, 1, FEARG_1,	    arg1_number,
+			ret_string,	    f_esp_serial_read},
+    {"esp_serial_write", 2, 2, FEARG_1,	    arg2_number_string,
+			ret_number,	    f_esp_serial_write},
+    {"esp_settings",	0, 0, 0,	    NULL,
+			ret_dict_any,	    f_esp_settings},
+    {"esp_ssh_get",	2, 3, FEARG_1,	    NULL,
+			ret_dict_any,	    f_esp_ssh_get},
+    {"esp_ssh_hostkey",	1, 1, FEARG_1,	    arg1_string,
+			ret_dict_any,	    f_esp_ssh_hostkey},
+    {"esp_ssh_keygen",	0, 2, FEARG_1,	    arg2_string,
+			ret_string,	    f_esp_ssh_keygen},
+    {"esp_ssh_list",	1, 2, FEARG_1,	    NULL,
+			ret_list_dict_any,  f_esp_ssh_list},
+    {"esp_ssh_mkdir",	1, 2, FEARG_1,	    NULL,
+			ret_bool,	    f_esp_ssh_mkdir},
+    {"esp_ssh_put",	2, 3, FEARG_1,	    NULL,
+			ret_dict_any,	    f_esp_ssh_put},
+    {"esp_ssh_remove",	1, 2, FEARG_1,	    NULL,
+			ret_bool,	    f_esp_ssh_remove},
+    {"esp_ssh_rename",	2, 3, FEARG_1,	    NULL,
+			ret_bool,	    f_esp_ssh_rename},
+    {"esp_ssh_trust",	1, 1, FEARG_1,	    arg1_string,
+			ret_bool,	    f_esp_ssh_trust},
+    {"esp_tasks",	0, 0, 0,	    NULL,
+			ret_list_dict_any,  f_esp_tasks},
+    {"esp_web_info",	0, 0, 0,	    NULL,
+			ret_dict_any,	    f_esp_web_info},
+    {"esp_web_passwd",	1, 1, FEARG_1,	    arg1_string,
+			ret_bool,	    f_esp_web_passwd},
+    {"esp_web_publish",	1, 1, FEARG_1,	    arg1_dict_any,
+			ret_void,	    f_esp_web_publish},
+    {"esp_web_settings_changed", 0, 0, 0,  NULL,
+			ret_bool,	    f_esp_web_settings_changed},
+    {"esp_web_start",	0, 1, FEARG_1,	    arg1_number,
+			ret_dict_any,	    f_esp_web_start},
+    {"esp_web_stop",	0, 0, 0,	    NULL,
+			ret_void,	    f_esp_web_stop},
+    {"esp_wifi_connect", 0, 2, FEARG_1,	    arg2_string,
+			ret_bool,	    f_esp_wifi_connect},
+    {"esp_wifi_disconnect", 0, 0, 0,	    NULL,
+			ret_void,	    f_esp_wifi_disconnect},
+    {"esp_wifi_forget",	0, 0, 0,	    NULL,
+			ret_void,	    f_esp_wifi_forget},
+    {"esp_wifi_saved",	0, 0, 0,	    NULL,
+			ret_string,	    f_esp_wifi_saved},
+    {"esp_wifi_scan",	0, 0, 0,	    NULL,
+			ret_list_dict_any,  f_esp_wifi_scan},
+#endif
     {"eval",		1, 1, FEARG_1,	    arg1_string,
 			ret_any,	    f_eval},
     {"eventhandler",	0, 0, 0,	    NULL,
